/* First C program -- based on current standard */
/* Earlier standards allow main() to be void type */
/* Current versions allows that, but throws a warning */

#include <stdio.h>


int main(){
	puts("C rocks!"); /* printf() is used when more formatting is needed */
	return 0;
}